---
tags: [watch, ux, workout, set-input, previous-sets]
date: 2026-02-28
category: brainstorm
status: draft
---

# Brainstorm: Watch 세트 입력 시 이전 세트 데이터 UI 개선

## Problem Statement

Watch 운동 중 세트 완료 후 무게를 바꾸려고 `SetInputSheet`을 열면, **상단에 Previous Sets 히스토리가 펼쳐져** 있어 무게 입력 영역이 아래로 밀린다. 세트가 쌓일수록 히스토리가 길어져 스크롤 없이는 무게 조작이 불가능해진다.

### 현재 구조 (SetInputSheet)

```
┌──────────────────────┐
│ Previous Sets        │ ← 세트가 쌓일수록 길어짐
│  Set 1  60.0kg ×10  │
│  Set 2  60.0kg ×10  │
│  Set 3  62.5kg ×8   │
│ ──────────────────── │
│      62.5            │ ← 무게 입력 (스크롤 필요)
│       kg             │
│  [-2.5]    [+2.5]    │
│ ──────────────────── │
│  [-]    8 reps  [+]  │
│              [Done]  │
└──────────────────────┘
```

### 핵심 불편

- Watch 화면(~40mm)에서 3세트 이상이면 무게가 화면 밖
- 세트 간 무게 변경은 **가장 빈번한 조작**인데 가장 접근하기 어려움
- Digital Crown 스크롤로 무게 입력까지 내려가야 하는 비효율

## Target Users

- Watch에서 직접 운동 기록하는 사용자
- 세트 간 무게를 자주 변경하는 사용자 (점진적 과부하, 드롭세트 등)

## Success Criteria

1. SetInputSheet 진입 시 **무게 입력이 즉시 보이고 조작 가능**
2. 이전 세트 데이터를 **필요할 때 1탭으로 확인 가능**
3. 세트 수 증가와 무관하게 **레이아웃 안정적**

## Proposed Approaches

### Option A: 좌상단 Toolbar 버튼 (사용자 선호)

```
┌──────────────────────┐
│ [📋]          [Done] │ ← 좌상단에 히스토리 버튼
│                      │
│      62.5            │ ← 무게가 항상 최상단
│       kg             │
│  [-2.5]    [+2.5]    │
│ ──────────────────── │
│  [-]    8 reps  [+]  │
└──────────────────────┘

 [📋] 탭 시 → sheet/popover로 Previous Sets 표시
```

**장점:**
- 무게 입력이 항상 최상단 — 즉시 접근
- 이전 데이터는 명시적 액션으로만 노출
- 세트 수와 무관하게 레이아웃 고정

**단점:**
- watchOS sheet 위에 또 sheet/popover가 가능한지 확인 필요
- 2탭 필요 (버튼 탭 → 데이터 확인 → 돌아가기)

**구현 방안:**
- `ToolbarItem(placement: .topBarLeading)` + `sheet(isPresented:)`
- 또는 `NavigationStack` 내부에서 `navigationDestination`으로 push

### Option B: 접히는 인라인 요약 (1줄)

```
┌──────────────────────┐
│                [Done] │
│ ▸ Last: 60.0kg ×10  │ ← 1줄 요약, 탭하면 펼침
│                      │
│      62.5            │
│       kg             │
│  [-2.5]    [+2.5]    │
│ ──────────────────── │
│  [-]    8 reps  [+]  │
└──────────────────────┘

 ▸ 탭 시 → DisclosureGroup 펼침
┌──────────────────────┐
│ ▾ Previous Sets      │
│  Set 1  60.0kg ×10  │
│  Set 2  60.0kg ×10  │
│ ──────────────────── │
│      62.5            │
│       ...            │
```

**장점:**
- 마지막 세트 무게가 항시 보여 참고 용이
- 1탭으로 상세 히스토리 확인
- 추가 sheet 없이 같은 화면에서 해결

**단점:**
- 접힌 상태에서도 1줄 공간 차지
- 펼치면 기존과 동일한 문제 재발
- watchOS DisclosureGroup 스타일이 제한적

### Option C: 레이아웃 순서 반전 (최소 변경)

```
┌──────────────────────┐
│                [Done] │
│      62.5            │ ← 무게가 최상단으로 이동
│       kg             │
│  [-2.5]    [+2.5]    │
│ ──────────────────── │
│  [-]    8 reps  [+]  │
│ ──────────────────── │
│ Previous Sets        │ ← 하단으로 이동 (스크롤)
│  Set 1  60.0kg ×10  │
│  Set 2  60.0kg ×10  │
└──────────────────────┘
```

**장점:**
- 구현 가장 간단 (VStack 순서만 변경)
- 무게/렙 입력이 항상 최상단
- 이전 데이터는 스크롤해서 확인 가능

**단점:**
- 이전 데이터 확인하려면 스크롤 필수
- "참고용으로 숨겨두기" 요구와 완전히 일치하지 않음 (항상 존재, 다만 하단)

### Option D: MetricsView에 마지막 세트 요약 + Sheet에서 제거

```
MetricsView (메인 화면):
┌──────────────────────┐
│ ═══════ 진행바 ═══   │
│     벤치프레스        │
│   Set 3 of 5  ●●○○○  │
│ Last: 60.0kg × 10   │ ← 마지막 세트 1줄 요약 추가
│ ┌──────────────────┐ │
│ │  62.5 kg         │ │
│ │  × 8 reps    ✏️  │ │
│ └──────────────────┘ │
│ [✓ Complete Set]     │
│ ♥ 142 bpm           │
└──────────────────────┘

SetInputSheet:
┌──────────────────────┐
│                [Done] │
│      62.5            │ ← Previous Sets 완전 제거
│       kg             │
│  [-2.5]    [+2.5]    │
│ ──────────────────── │
│  [-]    8 reps  [+]  │
└──────────────────────┘
```

**장점:**
- SetInputSheet이 순수 입력 용도로 최적화
- 이전 세트 참고는 sheet 열기 전에 해결
- 레이아웃이 가장 깔끔

**단점:**
- MetricsView 공간이 이미 빡빡함
- 세트 1에서는 "Last" 정보가 없음
- 상세 히스토리(여러 세트) 확인 불가

## Constraints

- watchOS 11+ (iOS 26 target)
- 화면 크기: ~40-49mm (제한적)
- Digital Crown은 무게 조작에 사용 중
- SetInputSheet은 `.sheet(isPresented:)` 으로 표시됨
- watchOS sheet 위에 sheet은 비권장 (fullScreenCover도 제한)

## Edge Cases

- **첫 세트**: Previous Sets 없음 → 버튼/섹션이 비활성 또는 숨김
- **세트 10개+**: Option C에서 히스토리가 매우 길어짐
- **bodyweight 운동**: weight=0, 이전 세트에 무게 없음

## Scope

### MVP (Must-have)
- 무게 입력이 SetInputSheet 진입 시 즉시 보이게
- 이전 세트 데이터 접근 경로 유지

### Nice-to-have (Future)
- 이전 "세션"(지난 주 같은 운동) 데이터 참고
- 무게 퀵 프리셋 (자주 쓰는 무게 버튼)

## Recommendation

**Option A (좌상단 Toolbar 버튼)** 를 1순위로 추천.

사용자 선호와 일치하며, 핵심 목표(무게 즉시 접근 + 이전 데이터 참고 가능)를 가장 잘 달성함. 구현 시 watchOS의 sheet-in-sheet 제한을 고려하여 `NavigationStack` 래핑 + `navigationDestination` push 패턴이 적절.

**Fallback**: Option A가 watchOS 제약으로 어려우면 Option C (순서 반전)가 가장 안전한 대안.

## Open Questions

1. watchOS에서 sheet 내부 `ToolbarItem(placement: .topBarLeading)` 이 정상 렌더되는지?
2. sheet 내부에서 `NavigationStack` → push 가 자연스러운지, 아니면 `fullScreenCover`가 나은지?
3. 이전 세트 히스토리 외에 "이전 세션(지난주)" 데이터도 향후 보여줄 계획이 있는지?

## Next Steps

- [ ] `/plan watch-set-input-previous-data-ux` 으로 구현 계획 생성
